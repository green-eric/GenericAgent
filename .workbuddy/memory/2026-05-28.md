# 2026-05-28 工作日志

## Bug 修复
- **otel_auto_trace.py `_patched_log` 签名不匹配修复**：`_patched_log` 只有 `(label, content)` 两个参数，但 `llmcore.py:1027` 调用时传了3个 `(label, content, log_path)`。添加了缺失的 `log_path=None` 参数（第141行），并在 `_orig_log` 调用时传递 `log_path`（第175行）。与 `langfuse_tracing.py:23` 保持一致。
