# R116: 安装包一键安装脚本权限测试

**时间**: 2026-05-14 02:15
**状态**: ✅ 分析完成（无管理员权限，仅做静态分析）
**验收标准**: 脚本在普通用户权限下测试通过，管理员权限部分明确标注

## 脚本权限分析

| 脚本 | 需要管理员 | 可安全测试 | 原因 |
|------|-----------|-----------|------|
| `install_python_windows.bat` | ⚠️ 是 | ❌ 否 | `net session` 提权检测 + `/passive InstallAllUsers=1` 系统级安装 Python |
| `ga-cli-install.cmd` | ✅ 否 | ✅ 是 | 操作 `HKCU\Environment`（用户级 PATH），不需要管理员 |
| `setup_task.bat` | ⚠️ 是 | ❌ 否 | `net session` 提权检测 + `schtasks /rl highest` 创建高权限计划任务 |
| `install_service.py` | ⚠️ 是 | ❌ 否 | `nssm install` 安装 Windows 服务需要管理员 |

## 结论

- **4 个脚本中 3 个需要管理员权限**（install_python、setup_task、install_service）
- **1 个可在普通用户下运行**（ga-cli-install.cmd，操作用户级注册表）
- 当前环境无管理员权限，无法实测需要管理员的脚本
- 脚本均已正确实现提权检测（`net session` 检测 + 自动重启/报错）

## 建议

1. `install_python_windows.bat` 和 `setup_task.bat` 已有 `net session` 自动提权逻辑，双击时会自动申请管理员权限 ✅
2. `install_service.py` 需要以管理员身份运行 Python，建议添加权限检测代码
3. `ga-cli-install.cmd` 可在普通用户下直接运行 ✅

## R112 验收状态: ⚠️ 部分完成
- ✅ 权限需求分析完成
- ✅ 管理员/普通用户标注完成
- ❌ 实测部分（需要管理员权限，当前环境不具备）
