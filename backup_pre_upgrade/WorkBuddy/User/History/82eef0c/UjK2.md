# 2026-04-25 日志

## stock_analyzer.py 文件恢复 (2026-04-26 01:39)

### 背景
stock_analyzer.py 源文件丢失（仅剩 __pycache__/stock_analyzer.cpython-314.pyc）。
尝试多种恢复方式均失败（无 git、无卷影副本、无 OneDrive、decompyle3 不支持 Python 3.14 pyc）。

### 恢复方式
基于对话历史 + README.md 文档 + 记忆文件，完整重建 stock_analyzer.py。

### 验证结果
- 语法检查: py_compile 通过 ✅
- 自测模式: 14/14 通过，0 失败 ✅
  - 年报段落提取正确
  - Q1/Q4 段落正确排除
  - 13 个财务指标解析全部正确
  - 净利润正确提取（非现金含量行）
  - 扣非净利润、经营现金流、OCF/净利润均正确

### 文件信息
- 路径: C:/Users/green/WorkBuddy/20260424203734/workplace/stock_analyzer.py
- 版本: V5.0.0
- 行数: ~650 行（精简重建版）

## README.md 与 stock_analyzer.py 一致性修正

发现并修正了文档与代码之间的多处不一致：

### 修正项
1. **行业补调分批参数**：文档写的"每批 50 只，批间休眠 3s"，实际代码是 `BATCH_SIZE=100`，休眠 `1s`。已修正。
2. **行业补调线程数**：文档未提及，代码 `INDUSTRY_API_WORKERS=15`。已补充到文档。
3. **HTTPAdapter 重试次数**：财务数据获取 `max_retries=2`，行业补调 `max_retries=1`。文档已区分。
4. **API_RETRY_TIMES 语义**：文档写的"API 重试次数"，实际代码 `range(1, API_RETRY_TIMES+2)` 总尝试 3 次（1 初始 + 2 重试）。已修正为"额外重试次数"。
5. **配置项补充**：补充了 `INDUSTRY_API_WORKERS` 和 `INDUSTRY_CACHE_DAYS` 到配置速查表。
6. **代码清理**：移除未使用的 `import subprocess`（V5 已改为 requests 直连）。

### 关键代码参数备忘
- `BATCH_SIZE = 100`（行业补调每批数量）
- 行业补调批间休眠 `1s`
- `INDUSTRY_API_WORKERS = 15`
- 行业补调 adapter `max_retries=1`，财务 adapter `max_retries=2`
- `API_RETRY_TIMES=2` → 总尝试 3 次

## V5 净利润解析bug修复 (22:58)

### Bug根因
`parse_financial_all()` 中净利润行匹配逻辑 `l.startswith('净利润')` 会错误匹配到 `净利润现金含量160.44%` 行（因为该行也以"净利润"开头）。由于 `净利润现金含量` 行出现在年报段落的"现金流状况"章节（早于"盈利能力"章节中的真实 `净利润` 行），导致总是匹配到错误的行。`_parse_num_from_line` 无法从百分比行提取带单位的数值，返回 None。

### 影响范围
- **2863只股票**（共4344只）的 `annual_net_profit` 为 null
- 连锁导致 `annual_ocf_to_profit`（经营现金流/净利润）也为 null
- 这些股票的现金流评分被默认为50分（缺失填充值），影响了评分准确性

### 修复方案
在行匹配条件中增加排除项：
```python
if l.startswith('净利润') and '归母' not in l and '扣非' not in l \
        and '现金含量' not in l and '增长率' not in l and '同比' not in l:
```

### 其他修复
1. **JSON输出添加 `data_timestamp`**：在JSON根节点加入数据生成时间戳
2. **JSON输出添加 `annual_report_date`**：每只股票包含年报日期
3. **Excel报告添加年报日期列**：格式化为 YYYY-MM-DD
4. **`merge_latest_reports` 补充 `annual_report_date`**：缓存命中的股票也能正确输出年报日期
5. **自测数据更新**：使用更接近真实NeoData格式的数据（含净利润现金含量行）

### 验证结果
- 自测通过（含净利润现金含量干扰项的正确识别）
- 5只股票实查验证：净利润全部正确解析，OCF/净利润计算正确
- 原有解析正确的字段（ROE、毛利率等）不受影响

## 第二轮文档同步 (23:16-23:52)

### 修正项
1. **净利润提取描述**：文档补充了"现金含量"/"增长率"/"同比"排除条件（代码第452行）
2. **自测模式描述**：补充了净利润现金含量干扰项的验证说明
3. **输出文件描述**：Excel增加"年报日期"列，JSON增加包装层结构说明
4. **代码清理**：
   - 移除未使用的 `import subprocess`（V4遗留）
   - 移除未使用的 `timedelta`/`wait`/`FIRST_COMPLETED`/`ALL_COMPLETED`/`Future`/`Any` 导入
   - 移除未使用的 `QUERY_SCRIPT`/`GLOBAL_DELAY`/`INDUSTRY_MAP_URL` 配置项
5. **V5核心变更描述**：补充清理遗留代码和输出格式变化说明

## V5 全量重跑验证 (23:23-00:03)

### 运行结果
- 全量 4344 只股票分析完成，耗时约 24 分钟
- 财务数据获取：4344/4344，0 API 错误（行业补调阶段有 322 条行业查询失败）
- 生成报告：`股票业绩评价_20260426_000327.xlsx`（1.3MB）
- 生成数据：`股票分析数据_20260426_000332.json`（4.3MB）

### 数据质量
- `data_timestamp`: 2026-04-26 00:03:32 ✅
- `annual_net_profit` 有值：2405/4344（55.4%），1939 只为 API 获取失败（fetch_success=False）
- `annual_ocf_to_profit` 有值：2405/4344（55.4%）
- `annual_report_date` 有值：2405/4344（55.4%）
- 行业覆盖率：4344/4344（100%）
- 评级覆盖率：4344/4344（100%）

### 评级分布
- A: 105（2.4%）/ B: 919（21.2%）/ C: 678（15.6%）/ D: 2272（52.3%）/ E: 370（8.5%）

### 错误分析（2261 条）
- `API错误码 error`：1939 次（85.7%）— NeoData API 调用失败（限流/超时/重试耗尽）
- `行业API补调 所有查询均失败`：322 次（14.3%）— 行业查询两次都未返回有效结果

### 评分验证
- 晓程科技（300139.SZ）：手动计算 88.01 分 A 级，与 JSON 完全一致 ✅
- 奥赛康（002755.SZ）：手动计算 87.42 分 A 级，与 JSON 完全一致 ✅
- 评分公式：行业百分位排名 → 盈利(ROE×0.4+毛利率×0.3+净利率×0.3) + 成长(营收同比×0.4+净利润同比×0.6) + 现金流 + 偿债

### 代码变更
1. **列名**：`"加权ROE(%)"` → `"ROE(%)"`（stock_analyzer.py 第 951 行）

### 待处理
- 1939 只 fetch_success=False 的股票全部被评为 D 级（33.75 分），因为所有指标为 None 时默认 50 分 × 低完整度惩罚 0.9×0.75=0.675 → 33.75 分
- 用户确认后续需要处理：在 calc_score 中加数据完整度门槛，低于门槛标记为"数据不足"跳过评级

## 季度报告评分方案评估 (01:01-01:31)

### 用户需求
- 目的：取年报评分A和季报评分A的**交集**作为优选股票
- 季报数据源：NeoData API（查询"年报"时自动返回多季度段落）

### NeoData季度数据实测结果
- 季度报告段落锚点格式：`统计截止日期为YYYYMMDD的Q1单季报/Q3单季报/Q4单季报`
- 季度报告字段完整度（7个核心指标）：
  - Q1: 3/7（毛利率、净利率、净利润同比）
  - Q3: 4/7（+营收同比）
  - Q4: 4/7（+营收同比）
  - 缺失字段：ROE、资产负债率、经营现金流

### 评估结论
- **推荐方案：季度数据 + 年报补充字段**
  - 季度提供：毛利率、净利率、营收同比、净利润同比
  - 年报补充：ROE、资产负债率、经营现金流/净利润
  - 评分权重不变，输出格式一致
- **选股逻辑**：年报A级 ∩ 季报A级 = 优选股票
- 预估交集规模：40-70只（年报A约105只，季报A预计80-120只）

### 搜索验证的外部策略
1. 兴业证券：4月是A股全年股价与业绩相关性最高的月份，年报+一季报密集披露期业绩为王
2. 量化私募：AI驱动多元数据交叉验证，跨季度会计政策比对，识别业绩粉饰
3. 戴维斯双击：低PE+成长性共振，季度业绩加速+年度ROE趋势确认
4. 质量因子(Quality Factor)：高ROE+低杠杆+稳定现金流+盈利可持续性
5. 业绩预增策略：同比增速+环比加速+超预期幅度，剔除已知业绩后未知增速>0

### 待确认问题（已提出给用户）
1. 季度数据时效性：2026Q1 + 2025年报跨年结合是否合理？
2. 行业评分基准：季度评分用季度数据股票还是沿用年报基准？
3. 是否需要单独展示"仅季报A"或"仅年报A"的股票？

### stock_analyzer.py 文件状态
- 文件不在磁盘上（可能在其他位置或需要重建）
- 代码逻辑完整记录在 README.md 中

## NeoData API 响应结构修复 (2026-04-26 01:46-01:49)

### Bug 根因
`run_neodata()` 函数尝试从 `data.text` 字段提取内容，但 NeoData API 实际返回结构为：
```json
{
  "code": "200",
  "data": {
    "apiData": {
      "apiRecall": [
        {"type": "财务主要复合指标", "content": "...统计截止日期为20251231的年报..."},
        {"type": "主营构成与业绩趋势", "content": "..."}
      ]
    }
  }
}
```
`data.text` 始终为空，导致所有股票返回空内容（0/5 成功）。

### 修复方案
重写 `run_neodata()` 的解析逻辑：
1. 优先从 `data.apiData.apiRecall` 数组中找包含"统计截止日期为...年报"的 content
2. 其次找 type 包含"财务"的 content
3. 最后拼接所有 content
4. 兼容旧格式 `data.text`（向后兼容）

### 验证结果
- 5/5 股票全部成功获取并解析 ✅
- 华测检测: ROE=14.07%, 毛利率=49.47%, 净利润=9.33亿
- 鹏鼎控股: ROE=11.73%, 净利润=36.19亿
- 卧龙电驱: ROE=8.15%, 净利润=8.32亿
- 华旺科技: ROE=14.92%, 净利润=5.65亿
- 东方精工: ROE=10.55%, 净利润=5.40亿

### 踩坑经验
- run_neodata / NeoData API 响应结构：实际是 `data.apiData.apiRecall` 数组，不是 `data.text`。代码中 `data["data"].get("text", "")` 始终返回空字符串。
